/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.ctfassets.net",
      "images.unsplash.com",
      "source.unsplash.com",
    ],
  },
};

module.exports = nextConfig;
